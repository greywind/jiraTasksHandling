import { createUseStyles } from "react-jss";

const styles = {
    wrapper: {
        padding: "20px 0",
    },
    value: {
        margin: "10px 0",
    },
};

export const useStyles = createUseStyles(styles);